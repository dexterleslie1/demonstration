version: "3.1"

services:
  nacos-server:
    image: nacos/nacos-server:v2.2.0
    environment:
      - TZ=Asia/Shanghai
      - MODE=standalone
    # 持久化配置
    # volumes:
    #  - data-demo-spring-boot-nacos:/home/nacos
    ports:
      - '8848:8848'
      - '9848:9848'
      #- '9849:9849'

# 持久化配置
# volumes:
#  data-demo-spring-boot-nacos:
