server.port=8080
spring.application.name=demo-spring-boot-mapdb

logging.level.root=info

# StreamingResponseBody 异步写出超时（毫秒），默认约 30 秒易触发 AsyncRequestTimeoutException
spring.mvc.async.request-timeout=600000
