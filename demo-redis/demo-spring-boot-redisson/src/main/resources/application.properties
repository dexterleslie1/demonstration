logging.level.root=info

# redis\u6A21\u5F0F\uFF1Astandalone\u3001replication\u3001sentinel\u3001cluster
spring.future.demo.redisson.redis.mode=${redis_mode:standalone}

# standalone\u914D\u7F6E
spring.future.demo.redisson.standalone.address=${standalone_address:redis://localhost:6379}
spring.future.demo.redisson.standalone.password=${standalone_password:123456}

# replication\u914D\u7F6E
spring.future.demo.redisson.replication.addresses=${replication_addresses:redis://localhost:6479,redis://localhost:6480,redis://localhost:6481}

# sentinel\u914D\u7F6E
spring.future.demo.redisson.sentinel.master-name=${sentinel_master_name:mymaster}
spring.future.demo.redisson.sentinel.addresses=${sentinel_addresses:redis://localhost:26579,redis://localhost:26580,redis://localhost:26581}

# cluster\u914D\u7F6E
spring.future.demo.redisson.cluster.addresses=${cluster_addresses:redis://localhost:6679,redis://localhost:6680,redis://localhost:6681,redis://localhost:6682,redis://localhost:6683,redis://localhost:6684}
