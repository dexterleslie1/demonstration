# 使用jvm模拟系统cpu、内存、io负载场景，然后使用系统和jvm相关命令找出负载问题

## jvm cpu负载分析

**结论：这种是cpu计算密集型负载，top命令显示的负载值很高。top命令us百分比非常高，sy、wa百分比几乎为0，系统长时间处理这种状态是稳定的**

```
# 使用jps命令找出当前运行的java进程
jps -mlv

# 使用top命令找出占用cpu线程
top -c -H -p [进程id]

```

### jvm内存负载分析

**结论：**

```

```

