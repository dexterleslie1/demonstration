## 组件库分类

在 uni-app 开发中，组件库可以根据其来源、维护方和使用方式分为多个类别。下面是对 **uni-app 组件库的分类详解**，帮助你清晰理解整个生态：

------

### 🧩 一、按组件来源分类

#### 1. **内置组件（Built-in Components）**

- **来源**：uni-app 框架原生提供
- **特点**：无需安装、开箱即用、跨平台兼容性好
- **常见组件**：
  - 视图容器：`<view>`, `<scroll-view>`, `<swiper>`
  - 基础内容：`<text>`, `<rich-text>`, `<progress>`
  - 表单组件：`<button>`, `<input>`, `<checkbox>`, `<radio>`, `<picker>`
  - 导航与媒体：`<navigator>`, `<image>`, `<video>`, `<camera>`
  - 地图与画布：`<map>`, `<canvas>`
- ✅ 使用方式：直接在 `.vue`文件中使用，无需 import 或 npm 安装

> 📌 文档参考：[uni-app 内置组件](https://uniapp.dcloud.net.cn/component/)

------

#### 2. **官方扩展组件库：uni-ui**

- **来源**：DCloud 官方团队维护
- **包名**：`@dcloudio/uni-ui`
- **特点**：
  - 提供更多高级 UI 组件（如日历、卡片、步骤条、悬浮菜单等）
  - 支持 easycom 自动引入，使用便捷
  - 需通过 npm 安装或在 HBuilderX 插件市场导入
- **典型组件**：
  - `<uni-calendar>`：日历选择
  - `<uni-card>`：卡片布局
  - `<uni-steps>`：步骤指示器
  - `<uni-badge>`：徽章
  - `<uni-datetime-picker>`：日期时间选择器
- ✅ 使用前提：安装依赖 + 配置 easycom（通常默认开启）

> 📌 文档参考：[uni-ui 官方文档](https://uniapp.dcloud.net.cn/component/uniui/uni-ui.html)

------

#### 3. **第三方 UI 组件库**

由社区或个人开发者提供的 uni-app 适配版 UI 库，通常基于 Vue 语法封装。

##### 常见第三方库举例：

| 名称                    | 简介                         | 特点                         |
| ----------------------- | ---------------------------- | ---------------------------- |
| **uView UI**            | 最流行的 uni-app UI 框架之一 | 功能丰富、文档完善、支持多端 |
| **ColorUI**             | 主打 CSS 样式库，轻量美观    | 适合快速美化界面，非组件化   |
| **ThorUI**              | 商业级组件库，设计感强       | 部分组件收费，适合电商类应用 |
| **TinyVue for uni-app** | 基于 Vue 3 的跨端组件库      | 支持 Vue3 + Vite 模式        |

- ⚠️ 使用方式：需下载源码或 npm 安装，可能需配置 easycom
- 🔧 注意：兼容性和维护性因库而异，建议选择活跃项目

------

#### 4. **自定义业务组件 / 私有组件**

- **来源**：开发者自己编写或从公司内部沉淀
- **用途**：复用页面模块（如订单卡片、弹窗、加载动画等）
- **存放位置**：通常在项目的 `components/`目录下
- ✅ 使用方式：局部注册或全局注册后使用

```
// 局部注册
import MyButton from '@/components/my-button.vue'
export default {
  components: { MyButton }
}
```

或使用 **easycom** 自动引入（推荐）：

```
// pages.json
"easycom": {
  "autoscan": true,
  "custom": {
    "^my-(.*)": "@/components/my-$1.vue"
  }
}
```

------

### 🗂️ 二、按功能用途分类（通用维度）

| 类别           | 示例组件                                    |
| -------------- | ------------------------------------------- |
| **基础展示**   | view, text, image, icon                     |
| **表单输入**   | input, picker, switch, slider               |
| **导航交互**   | navigator, button, tabbar                   |
| **列表滚动**   | scroll-view, list（nvue）, waterfall        |
| **多媒体**     | video, camera, live-player                  |
| **数据可视化** | ec-canvas（需集成 ECharts）、uchart         |
| **高级 UI**    | calendar, steps, badge, card（来自 uni-ui） |
| **布局辅助**   | grid, flex, float-layout                    |

------

### 🛠️ 三、按技术架构分类

| 类型                    | 说明                                                     |
| ----------------------- | -------------------------------------------------------- |
| **基于 Vue 2 的组件库** | 绝大多数现有库（如 uni-ui v1、uView 1.x）                |
| **基于 Vue 3 的组件库** | uView Plus（uView 2.x）、TinyVue 等，支持 Vite 构建      |
| **纯 CSS 样式库**       | ColorUI，仅提供类名样式，无 JS 逻辑                      |
| **原生渲染组件**        | nvue 专用组件（如 `<list>`, `<cell>`），性能更高但限制多 |

------

### ✅ 总结：如何选择？

| 场景             | 推荐方案                            |
| ---------------- | ----------------------------------- |
| 快速开发简单页面 | 使用 **内置组件** + 少量 **uni-ui** |
| 追求美观与效率   | 选用 **uView UI** 或 **ThorUI**     |
| 轻量级美化       | 使用 **ColorUI** 做皮肤             |
| 企业级定制       | 自研 **私有组件** + 结合 uni-ui     |
| Vue3 + Vite 项目 | 使用 **uView Plus**、**TinyVue**    |

------

### 📚 学习建议

1. 先熟练掌握 **内置组件**（80% 日常开发够用）
2. 根据项目复杂度引入 **uni-ui** 或第三方库
3. 避免过度依赖 UI 库，保持组件可维护性与性能
4. 善用 `easycom`实现组件自动引入，提升开发效率

## 组件库引入方式

>easycom引入方式具体参考本站示例：https://gitee.com/dexterleslie/demonstration/tree/main/demo-uni-app/demo-uview-getting-started

在 uni-app 中，引入组件的方式主要取决于 **组件的类型**（内置、uni-ui、第三方、自定义）以及 **项目构建方式**（HBuilderX 可视化创建 vs CLI 工程）。以下是 **uni-app 中各类组件的引入方式汇总**，按使用场景分类讲解：

------

### 🧩 一、内置组件（Built-in）—— 无需引入

#### ✅ 使用方式：**直接使用**

uni-app 的内置组件（如 `<view>`, `<button>`, `<text>`）是框架原生支持的，**无需任何引入操作**。

```
<template>
  <view class="box">
    <text>Hello uni-app</text>
    <button @click="handleClick">点击</button>
  </view>
</template>
```

> 📌 特点：跨平台、零配置、编译时自动识别。

------

### 📦 二、uni-ui 组件库引入方式

uni-ui 是 DCloud 官方扩展组件库，需安装依赖，但支持多种引入方式。

#### 方式 1：**npm 安装 + easycom 自动引入（推荐）**

##### 步骤：

1. 安装依赖：

   ```
   npm install @dcloudio/uni-ui
   ```

2. 确保 `pages.json`中启用 easycom（默认已开启）：

   ```
   // pages.json
   {
     "easycom": {
       "autoscan": true,
       "custom": {
         "^uni-(.*)": "@dcloudio/uni-ui/lib/uni-$1/uni-$1.vue"
       }
     },
     "pages": [...]
   }
   ```

3. 直接在页面中使用组件（无需 import）：

   ```
   <template>
     <uni-calendar :selected="selected" @change="onChange"></uni-calendar>
   </template>
   
   <script>
   export default {
     data() {
       return {
         selected: []
       }
     },
     methods: {
       onChange(e) {
         console.log('选中日期:', e)
       }
     }
   }
   </script>
   ```

> ✅ 优点：无需手动注册，开发效率高。

------

#### 方式 2：**手动 import 注册（适用于特殊场景）**

如果不想用 easycom，可以手动引入并注册组件：

```
<template>
  <uni-calendar :selected="selected" @change="onChange"></uni-calendar>
</template>

<script>
import uniCalendar from '@dcloudio/uni-ui/lib/uni-calendar/uni-calendar.vue'

export default {
  components: {
    uniCalendar
  },
  data() {
    return {
      selected: []
    }
  }
}
</script>
```

> 🔧 适用场景：动态加载、条件渲染、优化打包体积。

------

#### 方式 3：**HBuilderX 插件市场导入（非 CLI 项目）**

如果你是用 HBuilderX 创建的可视化项目（非 npm 工程）：

1. 打开 HBuilderX → 工具 → 插件安装 → 前往插件市场
2. 搜索 “uni-ui” → 安装到项目
3. 重启 HBuilderX，即可直接使用 `<uni-*>`组件（自动识别）

> ⚠️ 注意：这种方式不依赖 npm，但更新不便，适合初学者快速上手。

------

### 🎨 三、第三方 UI 组件库引入方式（以 uView UI 为例）

#### 方式 1：**npm 安装 + 配置 easycom（推荐）**

##### 步骤（以 uView 2.x 为例）：

1. 安装：

   ```
   npm install uview-plus
   ```

2. 在 `main.js`中引入（Vue3 示例）：

   ```
   import { createSSRApp } from 'vue'
   import uviewPlus from 'uview-plus'
   
   export function createApp() {
     const app = createSSRApp(App)
     app.use(uviewPlus)
     return { app }
   }
   ```

3. 配置 `pages.json`的 easycom：

   ```
   "easycom": {
     "autoscan": true,
     "custom": {
       "^u-(.*)": "uview-plus/components/u-$1/u-$1.vue"
     }
   }
   ```

4. 直接使用：

   ```
   <template>
     <u-button type="primary" @click="submit">提交</u-button>
   </template>
   ```

------

#### 方式 2：**完整引入（不推荐，影响性能）**

在 `main.js`中全局注册所有组件（不推荐用于大型项目）：

```
import uviewPlus from 'uview-plus'
app.use(uviewPlus)
```

> ⚠️ 会导致打包体积增大，建议配合 easycom 按需引入。

------

### 🧱 四、自定义组件引入方式

#### 方式 1：**局部注册（常用）**

在 `.vue`文件中单独引入：

```
<template>
  <my-button @click="handle">自定义按钮</my-button>
</template>

<script>
import myButton from '@/components/my-button.vue'

export default {
  components: {
    myButton
  }
}
</script>
```

------

#### 方式 2：**全局注册（适合高频复用）**

在 `main.js`中注册为全局组件：

```
// main.js
import { createApp } from 'vue'
import App from './App.vue'
import MyButton from '@/components/my-button.vue'

const app = createApp(App)
app.component('MyButton', MyButton) // Vue3
// Vue2: Vue.component('MyButton', MyButton)
app.mount('#app')
```

之后所有页面可直接使用 `<my-button>`。

------

#### 方式 3：**easycom 自动引入（强烈推荐）**

配置 `pages.json`实现组件自动扫描：

```
"easycom": {
  "autoscan": true,
  "custom": {
    "^my-(.*)": "@/components/my-$1.vue"
  }
}
```

目录结构示例：

```
components/
├── my-button.vue
├── my-card.vue
└── my-list.vue
```

使用：

```
<my-button></my-button>
<my-card></my-card>
```

> ✅ 优点：无需 import，自动注册，开发体验最佳。

------

### 🖼️ 五、纯 CSS 样式库引入（如 ColorUI）

ColorUI 是纯样式库，不涉及 JS 组件，只需引入 CSS 文件。

#### 引入方式：

1. 下载 ColorUI 源码，将 `colorui/`文件夹放入项目根目录

2. 在 `App.vue`中引入主样式：

   ```
   <style lang="scss">
   @import "colorui/main.css";
   @import "colorui/icon.css";
   /* 或使用 scss 变量 */
   @import "colorui/animation.css";
   </style>
   ```

3. 使用预设类名：

   ```
   <view class="cu-bar bg-blue">
     <view class="action">标题</view>
   </view>
   ```

> ⚠️ 注意：ColorUI 不提供组件逻辑，仅增强样式表现力。

------

### 📋 六、引入方式对比总结表

| 组件类型             | 引入方式              | 是否需要安装 | 是否需 import | 推荐指数 |
| -------------------- | --------------------- | ------------ | ------------- | -------- |
| 内置组件             | 直接使用              | ❌            | ❌             | ⭐⭐⭐⭐⭐    |
| uni-ui               | npm + easycom         | ✅            | ❌             | ⭐⭐⭐⭐⭐    |
| uni-ui               | 手动 import           | ✅            | ✅             | ⭐⭐⭐      |
| 第三方库（如 uView） | npm + easycom + use() | ✅            | ❌（easycom）  | ⭐⭐⭐⭐     |
| 自定义组件           | easycom 自动          | ❌            | ❌             | ⭐⭐⭐⭐⭐    |
| 自定义组件           | 局部注册              | ❌            | ✅             | ⭐⭐⭐      |
| 自定义组件           | 全局注册              | ❌            | ✅（一次）     | ⭐⭐⭐⭐     |
| CSS 库（ColorUI）    | 引入 CSS 文件         | ❌            | ❌             | ⭐⭐⭐      |

------

### ✅ 最佳实践建议

1. **优先使用 easycom**：减少重复代码，提升开发效率。
2. **内置组件为主**：满足大部分基础需求。
3. **uni-ui 作为补充**：官方维护，兼容性好。
4. **第三方库按需选择**：如 uView 适合复杂项目。
5. **自定义组件模块化**：结合 easycom 实现自动化管理。

## uView概念

>官方介绍：https://uviewui.com/components/intro.html

uView是uni-app生态专用的UI框架，uni-app 是一个使用 Vue.js 开发所有前端应用的框架，开发者编写一套代码， 可发布到iOS、Android、H5、以及各种小程序(微信/支付宝/百度/头条/QQ/钉钉)等多个平台(引言自uni-app网)。但目前除微信小程序，其它小程序平台的兼容可能存在一些问题，后续会针对这方面持续优化。

## uView集成到uni-app

>提示：使用easycom方式集成，创建uni-app项目时选择Vue版本为2。
>
>详细用法请参考本站示例：https://gitee.com/dexterleslie/demonstration/tree/main/demo-uni-app/demo-uview-getting-started

安装uView依赖

```sh
npm install uview-ui@2.0.37
```

main.js中注册uView到Vue中

```javascript
// 注册uView组件到Vue中
import uView from 'uview-ui'
Vue.use(uView)
```

pages.json配置uView的easycom

```json
{
	"easycom": {
		"autoscan": true,
		"custom": {
			"^u-(.*)": "uview-ui/components/u-$1/u-$1.vue"
		}
	},
    ...
}
```

uni.scss配置uView UI主题变量

```scss
/* uView UI 主题变量 */
@import 'uview-ui/theme.scss';
```

index.vue中调用uView组件

```vue
<template>
	<u-calendar :show="show" :mode="mode" @confirm="confirm" @close="()=>{show = false}"></u-calendar>
</template>

<script>
	export default {
		data() {
			return {
				show: true,
				mode: 'range'
			}
		},
		methods: {
			confirm(e) {
				console.log(e);
				this.show = false
			}
		}
	}
</script>

```

## uView - Calendar日历

>官方文档：https://uviewui.com/components/calendar.html
>
>详细用法请参考本站示例：https://gitee.com/dexterleslie/demonstration/tree/main/demo-uni-app/demo-uview-calendar

## uView - DatetimePicker

>官方参考链接：https://uviewui.com/components/datetimePicker.html
>
>具体用法请参考本站示例：https://gitee.com/dexterleslie/demonstration/tree/main/demo-uni-app/demo-uview-datetime-picker

## uView - Radio单选框

>官方参考链接：https://uviewui.com/components/radio.html
>
>具体用法请参考本站示例：https://gitee.com/dexterleslie/demonstration/tree/main/demo-uni-app/demo-uview-radio-group

## uni-ui概念

参考链接：https://uniapp.dcloud.net.cn/component/uniui/uni-ui.html

## uni-ui - uni-calendar日历

>说明：支持日期区间选择，支持农历日历。

参考链接：https://uniapp.dcloud.net.cn/component/uniui/uni-calendar.html

具体用法请参考本站示例：https://gitee.com/dexterleslie/demonstration/tree/main/demo-uni-app/demo-uni-calendar

## uni-ui - uni-datetime-picker日期选择器

>说明：支持日期、日期时间、日期区间、日期时间区间选择。

参考链接：https://uniapp.dcloud.net.cn/component/uniui/uni-datetime-picker.html

具体用法请参考本站示例：https://gitee.com/dexterleslie/demonstration/tree/main/demo-uni-app/demo-uni-datetime-picker