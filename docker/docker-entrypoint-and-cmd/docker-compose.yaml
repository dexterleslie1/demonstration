version: "3.0"

services:
  demo-test:
    build:
      context: .
    container_name: demo-test
    image: demo-test
    environment:
      - MARIADB_ROOT_PASSWORD=123456
    command: "88888888"
    # entrypoint: "date"
