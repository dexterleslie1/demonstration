version: "3.0"

# 创建网络
networks:
  net:
    name: demo-spring-boot-mybatis-net

services:
  # 启动mariadb
  demo-spring-boot-mybatis-db:
    container_name: demo-spring-boot-mybatis-db
    image: docker.118899.net:10001/yyd-public/demo-spring-boot-mybatis-db
    environment:
      - MYSQL_ROOT_PASSWORD=123456
      - TZ=Asia/Shanghai
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_general_ci --skip-character-set-client-handshake
    ports:
      - 3306:3306
    networks:
      - net