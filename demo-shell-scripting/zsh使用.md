# `zsh`使用

## `zsh`安装

### `ubuntu`安装`zsh`

- 安装`zsh shell`

  ```bash
  sudo apt install zsh
  ```

- 设置`zsh shell`为默认`shell`，其中`-s --shell`用于指定`shell`路径

  ```bash
  chsh -s /bin/zsh
  ```

- 重启系统加载`zsh shell`



