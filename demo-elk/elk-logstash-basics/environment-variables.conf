input {
    stdin {
        # 参考
        # https://www.elastic.co/guide/en/logstash/current/environment-variables.html
        add_field => {"$HOME environment variable" => "${HOME}"}
    }
}

output {
    stdout {

    }
}