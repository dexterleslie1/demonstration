server:
  port: 8083
spring:
  application:
    name: spring-cloud-helloworld
  cloud:
    consul:
      host: 127.0.0.1
      port: 8500
      discovery:
        service-name: ${spring.application.name}