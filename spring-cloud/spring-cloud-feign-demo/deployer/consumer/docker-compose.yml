version: "3.1"

services:
  service:
    image: registry.cn-hangzhou.aliyuncs.com/future-public/demo-spring-cloud-openfeign-consumer
    environment:
      - JAVA_OPTS=${java_opts}
      - consul_host=${consul_host}
      - TZ=Asia/Shanghai
    logging:
      driver: json-file
      options:
        max-size: "1g"
        max-file: "2"
    restart: unless-stopped
    network_mode: host
