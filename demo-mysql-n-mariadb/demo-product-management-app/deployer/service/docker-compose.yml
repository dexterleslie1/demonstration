version: "3.1"

services:
  service:
    image: registry.cn-hangzhou.aliyuncs.com/future-public/demo-product-management-app-service
    environment:
      - JAVA_OPTS=-Xmx4g
      - TZ=Asia/Shanghai
      - common_db_host=${common_db_host}
      - common_db_port=${common_db_port}
      - common_zookeeper_host=${common_zookeeper_host}
    restart: unless-stopped
    network_mode: host
