version: "3.0"

services:
  ui-vue:
    image: registry.cn-hangzhou.aliyuncs.com/future-public/demo-flash-sale-ui-vue
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ./nginx.conf:/usr/local/openresty/nginx/conf/nginx.conf:ro
    network_mode: host
    restart: unless-stopped
