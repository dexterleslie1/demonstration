version: "3.1"

services:
  service:
    image: registry.cn-hangzhou.aliyuncs.com/future-public/demo-spring-rocketmq-benchmark-crond
    environment:
      - JAVA_OPTS=-Xmx2g
      - TZ=Asia/Shanghai
    restart: unless-stopped
    network_mode: host
