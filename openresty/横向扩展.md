# `openresty`横向扩展方案

## 横向扩展方案

- 专业的负载均衡器+多个`openresty`实例

  可以在`openresty`前面部署一个专业的负载均衡器（如HAProxy、LVS等），该负载均衡器负责将请求分发到多个`openresty`实例上。

- `CDN`+多个`openresty`源站地址

  CDN回源策略配置：CDN服务商允许用户设置多个源站IP地址作为回源地址。这些源站IP可以添加到CDN的DNS设置中，每个源站IP可以具有不同的域名或CNAME记录。用户可以根据需要，以换行符等格式分割多个源站的IP，并配置CDN以轮询或其他策略来从这些源站中获取资源。

- `DNS`轮询技术+多个`openresty`实例

  在传统的DNS解析中，一个域名通常对应一个IP地址。但通过DNS轮询技术，可以实现一个域名对应多个IP地址。DNS服务器在接收到对该域名的解析请求时，会按照一定的顺序（如轮询方式）返回这些IP地址。客户端（如浏览器）在访问该域名时，会依次尝试这些IP地址，从而实现请求的分散和负载的均衡。



## `DNS`轮询技术+多个`openresty`实例

### 实验说明

此实验使用自定义`dns`服务器（单个域名`wwww.target321.com`指向多个`openresty`实例对应的`ip`地址）+`jmeter DNS Cache Manager`实现`DNS`轮询多个`openresty`实例效果。

### 实验结果

- 1台`openresty`服务器`CPU`被限制为`1000MHz`时，`QPS`约为`2000次/s`
- 2台`openresty`服务器`CPU`分别被限制为`1000MHz`时，请求被均衡分发到两台`openresty`服务器中，`QPS`约为`3400次/s`
- `todo` 从上面数据看，2台`openresty`服务器时不符合预期的`QPS`约为`4000次/s`（但是2台`openresty`时`QPS`也有所增长）

### 实验步骤

1. 搭建自定义`dns`服务器，配置域名`www.target321.com`指向多个`openresty`辅助压力测试目标，详细搭建步骤请参考 <a href="/linux使用/dns.html#centos8上搭建dns服务器" target="_blank">链接</a>
2. 基于`vsphere`搭建2个`openresty`辅助压力测试目标并分别限制其`CPU`为`1000MHz`，详细搭建步骤请参考 <a href="/性能测试/启动性能测试辅助目标.html" target="_blank">链接</a>
3. 搭建并配置`jmeter`分布式压测集群，详细搭建和配置步骤请参考 <a href="/jmeter/#基于kubernetes的jmeter分布式测试" target="_blank">链接</a>，注意：修改其中测试的`Http Request Defaults`的`Server Name or IP`配置为`www.target321.com`；新增`DNS Cache Manager`到测试中，`Options`选择`Use custom DNS resolver`，`DNS Servers`填写上面步骤搭建的`DNS`服务器`ip`地址。

