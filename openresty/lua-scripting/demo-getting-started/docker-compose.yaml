version: "3.0"

services:
  openresty:
    image: registry.cn-hangzhou.aliyuncs.com/future-public/openresty-base:1.1.1
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ./sample.conf:/usr/local/openresty/nginx/conf/nginx.conf:ro
    network_mode: host
